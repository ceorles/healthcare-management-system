import { useEffect, useState } from 'react';

export default function AuditLogs() {
    const [stats, setStats] = useState([]);

    return (
        <>
            <h1>AUDIT LOG NI ELVIRA</h1>
        </>
    );
}